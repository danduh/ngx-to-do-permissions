import { PermissionsPipe } from './permissions.pipe';

describe('PermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
